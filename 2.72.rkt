As ex2.70 described, for the most frequent symbols, it require 1 bit, and n -1 bits for least frequent symbols.

So, for n symbols to be encoded, the steps required to take for most and least frequent symbols are O(n) and O(n*n), respectively.